# Брендбук проекта Together

## Оглавление
1. [Типографика](#типографика)
2. [Цветовая палитра](#цветовая-палитра)
3. [Эффекты и анимации](#эффекты-и-анимации)
4. [Компоненты](#компоненты)
5. [Отступы и размеры](#отступы-и-размеры)
6. [Z-index уровни](#z-index-уровни)

---

## Типографика

### Основные шрифты

#### 1. Montserrat (Основной текст)
- **Использование**: Основной текст, формы, сообщения, описания
- **Начертания**: 400 (Regular), 600 (SemiBold), 700 (Bold), 800 (ExtraBold)
- **Примеры применения**:
  - Текст в body
  - Поля ввода (input, textarea, select)
  - Сообщения об ошибках и успехе
  - Статусные индикаторы

#### 2. Days One (Заголовки и акценты)
- **Использование**: Заголовки всех уровней, кнопки, карточки, важные элементы интерфейса
- **Начертания**: 400 (Regular), 600 (SemiBold), 700 (Bold)
- **Особенности**: 
  - Все заголовки (h1-h6)
  - Кнопки и элементы управления
  - Заголовки карточек квизов
  - Таймеры
  - Варианты ответов
- **Стили**: 
  - `text-transform: uppercase` для кнопок
  - `letter-spacing: 0.5px` для улучшения читаемости

#### 3. Caveat (Декоративный текст)
- **Использование**: Декоративные фразы, подписи, акцентные элементы
- **Начертания**: 400 (Regular), 600 (SemiBold), 700 (Bold)
- **Примеры применения**:
  - Фразы под логотипом (Frame 2)
  - Декоративные фразы в карточках квизов
  - Выделенная информация
- **Особенности**: 
  - Часто используется с `transform: rotate(-2deg)` для игривости
  - Цвет: `#354fba` (синий)

### Размеры шрифтов

Все размеры адаптивные через `clamp()`:

```css
--font-size-xs: clamp(12px, 2vw, 14px)    /* Мелкий текст */
--font-size-s: clamp(14px, 2.5vw, 16px)   /* Обычный текст */
--font-size-m: clamp(16px, 3vw, 18px)     /* Средний текст */
--font-size-l: clamp(20px, 3.5vw, 24px)   /* Крупный текст */
--font-size-xl: clamp(24px, 4vw, 32px)    /* Очень крупный текст */
```

#### Специфические размеры:

- **H1**: `clamp(36px, 6vw, 48px)`
- **H2**: `clamp(28px, 5vw, 36px)`
- **H3**: `clamp(24px, 4vw, 28px)`
- **Заголовки карточек**: `clamp(20px, 4vw, 30px)` - `clamp(24px, 5vw, 36px)`
- **Таймер**: `clamp(32px, 7vw, 48px)`
- **Кнопки**: `clamp(15px, 3vw, 17px)`
- **Варианты ответов**: `clamp(18px, 3.5vw, 22px)`

---

## Цветовая палитра

### Основные цвета

#### Фон и базовые
- **Фон сайта** (`--color-bg`): `#f2f2f2` (Светло-серый)
- **Белый** (`--color-white`): `#FFFFFF`
- **Черный** (`--color-black`): `#000000`
- **Темный** (`--color-dark`): `#393639` (Основной цвет текста)

#### Акцентные цвета

##### Коралловый (Coral)
- **Основной** (`--color-coral`): `#fb6c6b`
- **Альтернативный** (`--color-coral-alt`): `#fa5734`
- **Использование**: 
  - Ошибки и предупреждения
  - Акцентные элементы
  - Статусы "disconnected"
  - Таймер в срочном режиме

##### Зеленый (Green)
- **Основной** (`--color-green`): `#2fb95d`
- **Альтернативный** (`--color-green-alt`): `#2fb95d`
- **Использование**:
  - Успешные действия
  - Правильные ответы
  - Статусы "connected" и "ready"
  - Активные состояния

##### Лайм (Lime)
- **Основной** (`--color-lime`): `#abf67c`
- **Использование**:
  - Варианты ответов в квизе ГНУ
  - Акцентные элементы

##### Желтый (Yellow)
- **Основной** (`--color-yellow`): `#fb906b`
- **Использование**:
  - Предупреждения
  - Декоративные элементы

##### Синий (Caveat Blue)
- **Основной** (`--color-caveat`): `#354fba`
- **Использование**:
  - Декоративный текст в стиле Caveat
  - Ссылки (по умолчанию)
  - Акцентные фразы

#### Декоративные цвета (Floating elements)
- **Floating 1**: `#d6fbbf` (Светло-зеленый)
- **Floating 2**: `#fececd` (Светло-розовый)
- **Floating 3**: `#fb906b` (Оранжевый)

### Цвета для специфических квизов

#### Квиз ГНУ (зеленая тема)
- **Граница**: `#2fb95d`
- **Фон вариантов**: `rgba(171, 246, 124, 0.2)` - `rgba(171, 246, 124, 0.05)`
- **Метки**: `#abf67c` с различной прозрачностью

#### Квиз Академ (голубая тема)
- **Граница**: `#2b7a8a`
- **Фон вариантов**: `rgba(124, 232, 246, 0.2)` - `rgba(124, 232, 246, 0.05)`
- **Метки**: `#7ce8f6` с различной прозрачностью

### Состояния элементов

#### Кнопки и карточки
- **Фон по умолчанию**: `rgba(245, 245, 245, 0.85)`
- **Фон при наведении**: `rgba(245, 245, 245, 0.6)` - `rgba(245, 245, 245, 0.7)`
- **Граница**: `#393639` или `#000000`

#### Правильные ответы
- **Фон**: `rgba(47, 185, 93, 0.25)`
- **Граница**: `#2FB95D`
- **Метка**: `#2FB95D` с белым текстом

#### Неправильные ответы
- **Фон**: `rgba(250, 87, 52, 0.25)`
- **Граница**: `#FA5734`
- **Метка**: `#FA5734` с белым текстом

#### Статусы
- **Connected/Ready**: `#2fb95d` или `#00C159`
- **Disconnected/Waiting**: `#ff7664` или `#fa5734`

---

## Эффекты и анимации

### Тени (Box Shadow)

#### Стандартная тень (кнопки, карточки)
```css
box-shadow: 
    0 6px 0px rgba(0, 0, 0, 0.3),
    0 8px 16px rgba(0, 0, 0, 0.1);
```

#### Тень при наведении
```css
box-shadow: 
    0 8px 0px rgba(0, 0, 0, 0.4),
    0 12px 24px rgba(0, 0, 0, 0.15);
```

#### Легкая тень (формы, таймер)
```css
box-shadow: 
    0 4px 0px rgba(0, 0, 0, 0.2),
    0 6px 12px rgba(0, 0, 0, 0.1);
```

#### Тень при фокусе (формы)
```css
box-shadow: 
    0 4px 0px rgba(0, 193, 89, 0.3),
    0 8px 16px rgba(0, 193, 89, 0.15);
```

### Анимации

#### fadeInUp
- **Использование**: Появление карточек снизу вверх
- **Длительность**: 0.6s
- **Easing**: ease

#### fadeInDown
- **Использование**: Появление элементов сверху вниз
- **Длительность**: 0.3s - 0.6s

#### pulse
- **Использование**: Пульсация таймера в срочном режиме
- **Длительность**: 1s
- **Easing**: ease-in-out
- **Повтор**: infinite
- **Эффект**: Масштабирование от 1 до 1.05

#### gamepadPulse
- **Использование**: Подсветка вариантов ответа при использовании джойстика
- **Длительность**: 0.3s
- **Easing**: ease
- **Эффект**: Масштабирование от 1 до 1.05

#### iconFloat
- **Использование**: Плавающая анимация иконок в карточках
- **Длительность**: 3s
- **Easing**: ease-in-out
- **Повтор**: infinite
- **Эффект**: Движение вверх-вниз с легким поворотом

#### wobble
- **Использование**: Декоративные элементы (в настоящее время отключено)
- **Эффект**: Вращение и масштабирование

### Переходы (Transitions)

#### Стандартный переход
```css
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
```

#### Быстрый переход
```css
transition: all 0.2s ease;
```

#### Переход для трансформаций
```css
transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out;
```

### Трансформации

#### При наведении на кнопки/карточки
```css
transform: translateY(-6px) scale(1.02);
```

#### При активном состоянии
```css
transform: translateY(-4px) scale(0.98);
```

#### Декоративные повороты
```css
transform: rotate(-2deg);  /* Логотип, декоративные фразы */
```

### Эффекты размытия

#### Backdrop Filter
```css
backdrop-filter: blur(10px);
-webkit-backdrop-filter: blur(10px);
```
- **Использование**: Frame 4 и Frame 5 (футер)

### Градиенты

#### Радиальный градиент при наведении на карточки
```css
background: radial-gradient(circle, rgba(255, 255, 255, 0.3), transparent);
```

---

## Компоненты

### Кнопки

#### Основная кнопка (.btn, .frame-1-button)
- **Фон**: `rgba(245, 245, 245, 0.85)`
- **Цвет текста**: `#393639`
- **Граница**: `2px solid #393639` или `none`
- **Скругление**: `clamp(10px, 2vw, 12px)` - `12px`
- **Отступы**: `clamp(12px, 2.5vw, 14px) clamp(24px, 4vw, 32px)`
- **Шрифт**: Days One, 700, uppercase
- **Минимальные размеры**: 44px × 44px (для touch-устройств)
- **Тень**: Стандартная тень
- **Переход**: `all 0.3s cubic-bezier(0.4, 0, 0.2, 1)`

#### Состояния кнопок
- **Hover**: Поднятие на 6px, увеличение тени, изменение фона
- **Active**: Поднятие на 4px, уменьшение масштаба
- **Disabled**: Прозрачность 0.5, курсор not-allowed

### Карточки

#### Карточка квиза (.quiz-card)
- **Фон**: `rgba(245, 245, 245, 0.85)`
- **Скругление**: `clamp(12px, 2.5vw, 25px)` - `clamp(20px, 4vw, 25px)`
- **Отступы**: `clamp(18px, 4vw, 28px) clamp(16px, 3.5vw, 24px)`
- **Минимальная высота**: 280px (260px на мобильных)
- **Тень**: Стандартная тень
- **Анимация появления**: fadeInUp 0.6s

#### Структура карточки
- **Заголовок** (.quiz-title): Days One, 700, `clamp(20px, 4vw, 30px)`
- **Подзаголовок** (.quiz-subtitle): Days One, 400, `clamp(18px, 3.5vw, 22px)`
- **Описание** (.quiz-description): Days One, 400, `clamp(18px, 3.5vw, 22px)`, opacity 0.85
- **Фраза** (.quiz-phrase): Caveat, 400, `clamp(20px, 3.5vw, 28px)`, цвет `#354fba`, поворот -2deg
- **Информация** (.quiz-info): Days One, 600, `clamp(16px, 3vw, 20px)`, граница сверху 3px solid #000

### Варианты ответов

#### Кнопка варианта (.option-btn)
- **Фон**: `rgba(245, 245, 245, 0.9)`
- **Граница**: `clamp(3px, 0.6vw, 4px) solid #000`
- **Скругление**: `clamp(12px, 2.5vw, 16px)`
- **Отступы**: `clamp(16px, 3vw, 20px) clamp(20px, 4vw, 24px)`
- **Минимальная высота**: `clamp(60px, 10vw, 80px)`
- **Шрифт**: Days One, 600, `clamp(18px, 3.5vw, 22px)`

#### Метка варианта (.option-label)
- **Размер**: `clamp(40px, 8vw, 50px) × clamp(40px, 8vw, 50px)`
- **Фон**: `rgba(255, 255, 255, 0.9)`
- **Граница**: `clamp(2px, 0.4vw, 3px) solid #000`
- **Скругление**: `clamp(8px, 1.5vw, 10px)`
- **Шрифт**: Days One, 700, `clamp(20px, 4vw, 24px)`

### Формы

#### Поля ввода (input, textarea, select)
- **Фон**: `#FFFFFF`
- **Граница**: `clamp(2px, 0.5vw, 3px) solid #000`
- **Скругление**: `clamp(10px, 2vw, 12px)`
- **Отступы**: `clamp(12px, 2.5vw, 14px) clamp(14px, 3vw, 16px)`
- **Минимальная высота**: 44px
- **Шрифт**: Montserrat
- **Тень**: Легкая тень

#### Состояние фокуса
- **Граница**: `#2fb95d`
- **Тень**: Тень при фокусе (зеленая)

### Таймер

#### Основной таймер (.timer)
- **Фон**: `rgba(245, 245, 245, 0.85)`
- **Граница**: `clamp(2px, 0.5vw, 3px) solid #000`
- **Скругление**: `clamp(12px, 2.5vw, 16px)`
- **Шрифт**: Days One, 600, `clamp(32px, 7vw, 48px)`
- **Минимальная ширина**: `clamp(70px, 12vw, 80px)`

#### Срочный режим (.timer.urgent)
- **Фон**: `rgba(255, 118, 116, 0.35)`
- **Граница**: `#fb6c6b`
- **Цвет текста**: `#fb6c6b`
- **Анимация**: pulse

### Сообщения

#### Ошибка (.error)
- **Фон**: `#fa5734`
- **Граница**: `clamp(3px, 0.6vw, 4px) solid #000`
- **Цвет текста**: `#FFFFFF`
- **Шрифт**: Montserrat, 700

#### Успех (.success)
- **Фон**: `#2fb95d`
- **Граница**: `clamp(3px, 0.6vw, 4px) solid #000`
- **Цвет текста**: `#FFFFFF`
- **Шрифт**: Montserrat, 700

#### Предупреждение (.warning)
- **Фон**: `#fb906b`
- **Граница**: `clamp(3px, 0.6vw, 4px) solid #000`
- **Цвет текста**: `#393639`
- **Шрифт**: Montserrat, 700

---

## Отступы и размеры

### Система отступов

```css
--spacing-xs: 4px    /* Очень маленький отступ */
--spacing-s: 8px     /* Маленький отступ */
--spacing-m: 16px    /* Средний отступ */
--spacing-l: 24px    /* Большой отступ */
--spacing-xl: 32px   /* Очень большой отступ */
```

### Адаптивные отступы

#### Frame 1 (Панель кнопок)
- **Desktop**: `15px 40px`
- **Mobile**: `clamp(16px, 4vw, 24px)`
- **Очень маленькие экраны**: `clamp(12px, 3vw, 16px)`

#### Frame 2 (Логотип)
- **Отступ снизу**: `clamp(30px, 5vh, 60px)`
- **Mobile**: `clamp(20px, 4vh, 50px)`
- **Очень маленькие экраны**: `clamp(15px, 3vh, 40px)`

#### Frame 3 (Контент)
- **Отступ сверху**: `clamp(80px, 12vh, 120px)`
- **Mobile**: `clamp(40px, 6vh, 60px)`
- **Очень маленькие экраны**: `clamp(35px, 5vh, 50px)`
- **Боковые отступы**: `clamp(16px, 4vw, 24px)`

#### Frame 4 и Frame 5 (Футер)
- **Отступы**: `clamp(15px, 3vw, 25px) clamp(16px, 4vw, 24px)`
- **Mobile**: `clamp(12px, 3vw, 16px)`

### Максимальные ширины

- **Контент**: 1200px
- **Frame 2 контент**: 1150px (≥768px), 90% (<768px)
- **Frame 1 контент**: 1400px

---

## Z-index уровни

Система z-index для правильного наложения элементов:

```css
--z-index-base: 1
--z-index-decor: -1                    /* Декоративные элементы */
--z-index-content: 1002                /* Основной контент */
--z-index-frame-4: 999                 /* Frame 4 (футер) */
--z-index-frame-2: 1000                /* Frame 2 (логотип) */
--z-index-frame-1: 10000               /* Frame 1 (кнопки) */
--z-index-character-menu: 9999         /* Меню персонажей */
```

### Порядок наложения (снизу вверх)
1. Декоративные элементы (-1)
2. Frame 4 (999)
3. Frame 2 (1000)
4. Основной контент (1002)
5. Меню персонажей (9999)
6. Frame 1 (10000)

---

## Адаптивность

### Брейкпоинты

- **Mobile**: `max-width: 767px`
- **Tablet**: `768px - 1023px`
- **Desktop**: `min-width: 1024px`
- **Очень маленькие экраны**: `max-width: 480px`
- **Минимальные экраны**: `max-width: 360px`

### Принципы адаптивности

1. **Все размеры используют `clamp()`** для плавного масштабирования
2. **Минимальные размеры touch-элементов**: 44px × 44px
3. **Адаптивные отступы**: Используются vw и vh единицы для устойчивости к масштабированию
4. **Гибкая типографика**: Размеры шрифтов адаптируются к ширине экрана
5. **Симметричные отступы**: На мобильных устройствах отступы симметричны для визуального баланса

---

## Дополнительные заметки

### Особенности дизайна

1. **Игривость**: Использование поворотов (-2deg) для логотипа и декоративных элементов
2. **Глубина**: Многослойные тени создают ощущение объема
3. **Интерактивность**: Все интерактивные элементы имеют четкие состояния hover и active
4. **Доступность**: Минимальные размеры 44px для touch-устройств, контрастные цвета
5. **Производительность**: Использование `will-change` и `transform` для плавных анимаций

### Рекомендации по использованию

1. **Шрифты**: Всегда используйте указанные шрифты для соответствующих элементов
2. **Цвета**: Используйте CSS-переменные для цветов для легкого обновления
3. **Отступы**: Следуйте системе отступов для консистентности
4. **Анимации**: Не перегружайте интерфейс анимациями, используйте их осознанно
5. **Адаптивность**: Всегда тестируйте на разных размерах экранов

---

*Последнее обновление: 2025*
